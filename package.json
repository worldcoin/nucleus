{
  "name": "@worldcoin/nucleus",
  "version": "0.0.5",
  "private": true,
  "description": "Design tokens for the World ecosystem",
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsx tokens/build/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "format": "prettier --write \"**/*.json\"",
    "format:check": "prettier --check \"**/*.json\"",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "handlebars": "^4.7.9",
    "style-dictionary": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "oxlint": "^1.51.0",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
