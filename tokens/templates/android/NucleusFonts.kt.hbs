// This file is auto-generated. Do not edit manually.

package {{packageName}}

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.TextUnit
import androidx.compose.ui.unit.em
import androidx.compose.ui.unit.sp
import com.worldcoin.nucleus.R

/**
 * A nucleus design-token font.
 *
 * Each value carries enough information to construct a Compose `TextStyle` directly. Nucleus stays unopinionated about how the font is applied. Pick the fields you need and pass them into your own `TextStyle` / `Typography` setup.
 */
data class NucleusFontStyle(
    val fontFamily: FontFamily,
    val fontSize: TextUnit,
    val fontWeight: FontWeight,
    val letterSpacing: TextUnit,
    val lineHeight: TextUnit,
)

object NucleusFonts {
{{#each families}}
    object {{name}} {
        private val fontFamily = FontFamily(Font(R.font.{{resourceName}}))

{{#each tokens}}
        val {{name}} = NucleusFontStyle(
            fontFamily = fontFamily,
            fontSize = {{size}}.sp,
            fontWeight = FontWeight({{weight}}),
            letterSpacing = ({{letterSpacing}}).em,
            lineHeight = ({{lineHeight}}).em,
        )
{{/each}}
    }
{{/each}}
}
